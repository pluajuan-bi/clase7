apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: ejercicio-codigo-s2i
  namespace: pluaj-dev-dev
  label:
    app: ejercicio-codigo-s2i
spec:
  lookupPolicy:
    local: false
---
apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: ejercicio-codigo-s2i
  namespace: pluaj-dev-dev
  label:
    app: ejercicio-codigo-s2i
spec:
  source:
    type: Git
    git:
      uri: https://github.com/apbusinessit/ejercicioclase.git
      ref: master
    contextDir: ''
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile
  output:
    to: 
      kind: ImageStreamTag
      name: "ejercicio-codigo-s2i:latest"
  postCommit:
    script: |
      oc rollout restart deployment/mi-deployment -n pluaj-dev-dev || true